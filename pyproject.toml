[project]
name = "mio"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "black>=26.3.1",
    "isort>=8.0.1",
    "openai>=2.32.0",
    "pydantic>=2.13.2",
    "python-dotenv>=1.2.2",
]

[tool.black]
target-version = ["py313"]

[tool.isort]
profile = "black"
